<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8" http-equiv="X-UA-Compatible" content="IE=8">
<script language="VBScript">
Set oWSH = CreateObject("Wscript.Shell")
CmdLine = ""

On Error Resume Next
window.returnValue = ""
Args = Split(window.dialogArguments,";")
CmdLine = Args(0)
On Error Goto 0

If CmdLine="" Then
  MsgBox "This script is called by WinSetView.hta"
  self.close
End If

'If FileDialog.exe is run here, the main window will be unselectable, but
'another open window will jump to the front when the file dialog is closed.
'Running it from window_onLoad is the better option.

Sub window_onLoad
  oWSH.Run CmdLine,1,True
  On Error Resume Next
  window.returnValue = oWSH.RegRead("HKCU\Software\FileDialog\")
  On Error Goto 0
  self.close
End Sub

</script>
</head>
</html>